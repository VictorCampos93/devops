pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'ls -l'
        sh 'echo "iniciando o build"'
      }
    }
    stage('Tests') {
      steps {
        sh 'pwd'
      }
    }
  }
}